/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running minimal tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Defines input files and everything required to run a fast and simple pipeline test.

    Use as follows:
        nextflow run fulcrumgenomics/twistcgp -profile test,<docker/singularity> --outdir <OUTDIR>

----------------------------------------------------------------------------------------
*/

process {
    resourceLimits = [
        cpus: 2,
        memory: '4.GB',
        time: '10.m',
    ]
    withName: SNPEFF_SNPEFF {
        // This process fails with the empty output from GATK4_MUTECT2
        // TODO: Create small test data to use in integration testing
        ext.when = { false }
    }
    withName: '.*:SNPEFF_DOWNLOAD' {
        // This process takes more than 10 minutes; we will skip it for now
        ext.when = { false }
    }
    withName: TABIX_BGZIPTABIX {
        // This process runs inside VCF_ANNOTATE_SNPEFF (after SNPEFF_SNPEFF, which we are also skipping).
        // In the main workflow it is gated by skip_tmb, so skip_tmb = true above already prevents that invocation.
        ext.when = { false }
    }
    withName: '.*:ENSEMBLVEP_DOWNLOAD' {
        // This process takes more than 10 minutes; we will skip it for now
        ext.when = { false }
    }
    withName: '.*:ENSEMBLVEP_VEP' {
        // This process fails with the empty output from GATK4_MUTECT2
        // TODO: Create small test data to use in integration testing
        ext.when = { false }
    }
    withName: "GATK4_FILTERMUTECTCALLS" {
        // FilterMutectCalls fails with the minimal test data
        ext.when = { false }
    }
    withName: "CIVICPY_UPDATE_CACHE" {
        // Downloading the CIViC cache is too slow for CI
        ext.when = { false }
    }
}

params {
    skip_tmb = true
    skip_msi = true
    config_profile_name = 'Test profile'
    config_profile_description = 'Minimal test dataset to check pipeline function'

    // Input data
    input = params.pipelines_testdata_base_path + 'samplesheet.csv'
    baits = params.pipelines_testdata_base_path + 'panel.intervallist'
    targets = params.pipelines_testdata_base_path + 'panel.bed'

    // nf-core modules test-data
    modules_testdata_base_path = 'https://raw.githubusercontent.com/nf-core/test-datasets/modules/data/'
    fasta = params.pipelines_testdata_base_path + 'reference.fa'
}
